name = "extension"
description = "Browser extension surface"

red_lines = [
  "The extension never bypasses the backend gateway for storage",
]

[[modules]]
name = "extension"
keywords = "extension,content-script,popup"
code = "apps/extension"
