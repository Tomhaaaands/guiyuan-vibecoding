# R76 · responsibility-modular-vcm（2026-09-06）

> Full iteration record for the VCM responsibility-module refactor.

## Conclusion

VCM now keeps one public `guiyuan-vibecoding` Skill while physically separating the repository
runtime into core, requirement, planning, workflow, QA, release, install, and uninstall packages.
The workflow router is the sole orchestration layer and modules exchange a versioned result
envelope plus artifact references. Root `tools/*.py` paths remain compatible wrappers.

## Verification

- `python -m unittest discover -s tests -t .` — 191 tests passed.
- Added module contract documentation and explicit Skill routing; design remains an optional future
  `guiyuan-design` seam.
- Release behavior remains local/dry-run; no tag, push, or GitHub write was executed.
