# R71 — Public Skill boundary and duplicate discovery cleanup

- 全局对外入口收敛为 `guiyuan-vibecoding`；install 与 iteration close-loop 改为显式内部路由。
- close-loop 载荷不再使用嵌套 `SKILL.md` 文件名；bootstrap 在项目选择该流程时恢复项目本地 `SKILL.md`。
- 安装器更新为只安装主 Skill，并把旧的两个全局入口迁移到发现根目录外；Windows junction 只解除链接，内容保留。
- 分发 ZIP 根目录改为只含一个公共 Skill；manifest、doctor、漂移检查、模板与文档同步更新。
- 验证：33 项定向测试、177 项全量测试、`python tools/check_drift.py`、`python tools/build_dist.py --verify`、当前 Codex Skill 根目录盘点均通过。
