# R5 · tooling (2026-08-29)

v0.3.0 self-iteration tooling: hydrate `--semantic` reserved interface (HYDRATE_SEMANTIC_BACKEND,
graceful keyword fallback); install_skills.py `--doctor` self-check + VERSION file; distill.py +
docs/distillation.md define the four-direction distillation loop
(pitfalls/method/consolidate/promote, implementation deferred until the shared memory/embedding
service stabilizes); methodology/README updated; template+asset copies kept identical; verified
doctor + check_drift pass.
