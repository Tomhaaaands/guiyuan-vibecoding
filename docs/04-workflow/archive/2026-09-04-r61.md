# R61 · static-project-home（2026-09-04）

> Full iteration records (archive volumes of the changelog ledger). Not read daily; archaeology enters via `../changelog.md`.
> Generated by tools/rollup_round.py.

## Root cause

The existing visual project-home template was only a design artifact while the installed project
payload still rendered a separate plain status page and offered an unnecessary 8010 server.

## Implementation

- Added the visual template and Tao Yuanming assets to the reusable iteration template and all
  install payload copies.
- Reworked `tools/render_project_home.py` to derive identity, modules, roadmap, architecture,
  principle flow, document tree, and Git metadata, then replace only `PROJECT` in the template.
- Inlined decorative images into the generated file and added generated-link validation.
- Bootstrap renders `status.html` after scaffold/adopt; `rollup_round.py` regenerates it after each
  ledger update. Removed the server scripts from the managed-project payload.
- Updated install doctor, product/technical docs, README, and the template index.

## Verification

- Static render on the repository and a temporary scaffold project succeeded.
- Targeted renderer, bootstrap detection, and install lifecycle tests passed.
- `python tools/build_dist.py --verify` passed; zip contains the project-home template and no
  `serve_project`/`serve_status` files.
- `python tools/check_drift.py` passed (startup budget remains a non-blocking warning).
- `python tools/run_qa.py` passed: unittest, behavior, drift, package, architecture, and release
  checks all PASS.
