# R45 · topology, settings, migration (2026-09-03)

## Conclusion

- Added a reference-material capture rule: raw research stays in `03-reference`, while accepted
  conclusions are back-synced into system/product/technical authority docs.
- Added `apps/` and `workers/` placeholders and required five-layer docs placeholders to internal
  and published scaffolds.
- Added read-only `tools/architecture_audit.py` and a migration protocol for legacy layouts and
  parallel authority conflicts.
- Added the first Admin Settings page for provider and migration-confirmation settings.

## Verification

- Run `python tools/sync_copies.py`, `python tools/check_drift.py`, and `python tools/build_dist.py --verify`.
