name = "saas"
description = "Multi-tenant: every row user-scoped; billing idempotent"

red_lines = [
  "Every table carries user_id (or tenant_id); cross-user access returns 404",
  "Billing deductions are idempotent via ref_id; failures roll back",
]

constraints = [
  "API keys and secrets never leave the backend",
]

docs_stubs = ["00-system/data-layer.md", "00-system/deployment.md"]

[[modules]]
name = "billing"
keywords = "billing,plan,quota"
code = "apps/api"

[[modules]]
name = "account"
keywords = "account,tenant,org"
code = "apps/api"
