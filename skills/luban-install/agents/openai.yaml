interface:
  display_name: "Luban One-Click Install"
  short_description: "One-click install/update of 鲁班 skills + optional project scaffold"
  default_prompt: "Use $luban-install to install or update the 鲁班 kit skills and optionally scaffold a project folder."
policy:
  allow_implicit_invocation: false
