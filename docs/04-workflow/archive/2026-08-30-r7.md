# R7 · distill (2026-08-30)

`tools/distill.py` powered on. `pitfalls` (坑 → 红线) implemented as a deterministic first pass:
scans a project's archive volumes + module `iteration.md` for pitfall/red-line/incident markers,
dedupes, and emits `[AI-DRAFT]` candidates to `red-lines.draft.md` (dry-run by default; never
auto-edits the authoritative `red-lines.md`).

Removed the wrong memory-os/embedding-service dependency — distillation reads project archives
only. `method` / `consolidate` / `promote` remain stubs. Tool synced to template + asset copies.

Verified against tom_creator_os: 135 files scanned → 20 deduped candidates, `check_drift` green.
