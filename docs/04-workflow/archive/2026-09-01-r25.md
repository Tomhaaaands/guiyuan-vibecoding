# R25 · dual GitHub proxy fallback

## Background

The domestic mirror now uses Gitee raw as the primary non-login path, with `gh-proxy.com` and
`ghfast.top` as verified GitHub proxy fallbacks. All paths still require SHA-256 verification.

## Verification

- Both proxies downloaded the `0.1.0` ZIP and `.sha256` successfully.
- Proxy ZIP hashes match the official SHA-256.
- Install skill and docs list the fallback order.
