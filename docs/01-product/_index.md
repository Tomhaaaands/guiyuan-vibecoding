# Product Capability Index

产品文档按用户能力拆分。每个能力目录至少包含 `prd.md` 和 `acceptance.md`；用户可见流程
放在 `ux.md`，不可见但有产品承诺的系统行为放在 `behavior.md`。

技术实现不在这里一一复制，而是通过 `supports` / `depends_on` 关系连接到 `docs/02-technical/`。
