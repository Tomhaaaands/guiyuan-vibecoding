# R43 - 方法论落档 + 旧打包归档 + GitHub release 删除

## 结论

- 把 P0-P8 本地闭环写进 `docs/iteration-methodology.md`（§5a Manager local loop，含各阶段引擎与验收）。
- 归档旧打包：GitHub 发布 zip 分发停用；仓库直装设为默认；install-by-message 改为「自行
  `build_dist` 构建并托管后发消息」。更新 README / README.zh-CN / install skill / release-sources /
  可视化 HTML，移除全部指向已删除 release 的 URL。
- 从活动树移除 `releases/0.1.0/`（zip/sha256/manifest，git 历史保留），并删除 GitHub `v0.1.0`
  release（git tag 保留）。

## 验证

- `check_drift.py` -> 全绿（标记 0/0、启动 1584/1800、模板/资产同步一致、版本单一）。
- `behavior_harness.py` -> 20/20；`py_compile` ok；密钥 0 命中。
- 全仓库对 `releases/download/v0.1.0` / `vibecoding-manager-0.1.0.zip` 只剩两处「归档/停用」说明
  文字，无活动引用。
