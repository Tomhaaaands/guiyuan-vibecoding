name = "c-end"
description = "Consumer-facing: auth, payment, push, compliance"

red_lines = [
  "Payment state changes are idempotent and auditable (order_id)",
  "User PII is not logged; compliance rules are product-level",
]

constraints = [
  "Sessions use httpOnly cookies; tokens never live in localStorage",
]

docs_stubs = ["01-product/compliance.md"]

[[modules]]
name = "auth"
keywords = "login,register,session"
code = "apps/api"

[[modules]]
name = "payment"
keywords = "payment,order,refund"
code = "apps/api"
