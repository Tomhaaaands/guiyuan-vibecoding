# R10 · naming (2026-08-30)

技术名显示层统一（延续 R9 改名）：

- kit 内对私人管家的引用从 `memory-os` 统一为 **Private_butler**（AGENTS.md、distill.py ×3、
  iteration-methodology.md ×3、distillation.md、fact-ownership 双镜像示例行）。
- fact-ownership 示例同步新名（Creator OS → Flash_assistant）。
- vibe-coding-install 内置 project-bootstrap 副本经同步门禁发现漂移后 robocopy /MIR 刷新。

验证：check_drift 4 对同步全绿、doctor 通过。
