# R9 · naming (2026-08-30)

三系统统一命名（用户拍板），本仓库对应 **VibeCoding_Manager**：

- `_bootstrap` / 鲁班 → **VibeCoding_Manager**；技术 repo 名 `project_bootstrap` 与 skill 触发词
  `project-bootstrap` / `iteration-close-loop` 保留。
- `luban-install` skill 改名 `vibe-coding-install`（触发词 `$vibe-coding-install`）：
  SKILL.md / openai.yaml / install.py / install_skills.py / check_drift 同步门禁路径全部对齐，
  $CODEX_HOME 旧目录移除、重装。
- README 标题与文案、AGENTS.md、docs/iteration-methodology.md（三副本）、
  docs/distillation.md、模板 README/AGENTS.md、工具输出文案全部替换。
- 同步门禁 4 对全绿；`llms.txt` 以新名重生成。

配套命名：Private_butler（memory_os，白泽/Mnemos）、Flash_assistant（tom_creator_os，文曲星/Creator OS）。
