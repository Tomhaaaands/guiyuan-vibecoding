# R21 · public-release closure (2026-09-01)

## Outcome

`Tomhaaaands/vibecoding-manager` is public. `main` points at the verified release commit and
annotated tag `v1.2.0` anchors the first public version. Its GitHub Release publishes the
install-by-message ZIP, `.sha256`, and JSON manifest.

## Verification

- `check_drift.py` and `check_package.py` passed immediately before release.
- The release ZIP was rebuilt from the clean tagged commit, then downloaded from GitHub; its
  SHA-256 was `d3c20e87b00dec3a4e03a9fa37921d3731628daa9530d66fa327169238af4bb6` and the extracted
  root contained exactly `iteration-close-loop`, `vibe-coding-manager`, and `vibe-coding-install`.
- `tools/install_skills.py --force` installed the three current Codex skills and backed up the
  prior copies under `.vibecoding-manager-backups/20260831T190327Z`; `--doctor` passed.
- No active `project-bootstrap` Skill or MCP configuration remains. The obsolete Codex trust entry
  for the old project path was removed.

## Boundary

The `v1.2.0` tag and uploaded Release assets are not replaced. Future changes increment
`VERSION` and produce a new tag and asset set.
