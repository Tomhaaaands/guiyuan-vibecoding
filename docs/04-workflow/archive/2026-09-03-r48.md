# R48 · internal QA suite + published project self-check (2026-09-03)

## Conclusion

- Built a stdlib-only internal test suite under `tests/` covering the artifact store, context
  compiler/budget, analysis, provider registry/labels, artifact consistency/generation, task
  graph, receipt loop, experience loop, MVP walkthrough, distillation, hydration, round rollup,
  and the delivery/package gates (unittest + real-CLI subprocess).
- Added `tools/run_qa.py` as the single internal QA gate, aggregating unittest, the behavior
  harness, `check_drift`, `build_dist --verify`, `architecture_audit`, and `check_package`, with
  optional stdlib `--coverage` and `--frontend` smoke; it writes `.qa/qa-report.json`.
- Extracted a project-agnostic `tools/selfqa.py` into the published template and synced it to the
  manager skill asset payload; it verifies an adopted project's tools, AGENTS contract, docs
  skeleton, drift/budget/links/markers, and declared red-lines, and is stdlib-only.
- Made skill install transactional in both `tools/install_skills.py` and the bundled
  `skills/vibe-coding-install/scripts/install.py`: pre-check, backup to
  `.vibecoding-manager-backups/<stamp>`, atomic swap via staging dir, post-install validation, and
  restore-on-failure with a retained backup report.
- Documented the QA boundary in `docs/02-technical/qa-contract.md` and routed it from `AGENTS.md`;
  regenerated `llms.txt` and re-synced all release payload copies.

## Verification

- `python -m unittest discover -s tests -t .` -> 141 tests passed.
- `python tools/run_qa.py` (and `--coverage`, `--frontend`) -> all gates passed.
- `python tools/sync_copies.py` -> template/asset payloads in sync; `check_drift.py` ->
  doc-drift check passed.
- Published zip contains both `selfqa.py` copies; `build_dist --verify` passes.
