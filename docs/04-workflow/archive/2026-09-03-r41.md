# R41 - P6 经验候选 + 影子策略评估

## 结论

- 新增 `tools/experience_loop.py`：从 `receipts`（verdict=fail/blocked）提取教训，按任务分组，
  写成 `[AI-DRAFT]` 的 `experience` 候选（status=draft，depends_on=证据 receipt，永不自动升 accepted）。
- 影子评估 `shadow_evaluate(min_frequency)`：仅推荐候选，不修改权威 `red-lines.md`；可选
  `--draft` 只写 `red-lines.draft.md`（复核用），与 distill 同规则。
- 一次重复失败 -> 一条证据支持的经验候选；绝不由 AI 直接落生产红线。

## 验证

- `behavior_harness.py` -> 19/19 pass（新增 `experience_loop`：blocked 回执 -> 唯一年份候选 ->
  draft experience + [AI-DRAFT] + 影子评估命中 + 未触碰权威 red-lines.md）。
- `py_compile` ok；`check_drift.py` 绿。
