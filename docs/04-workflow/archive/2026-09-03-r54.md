# R54 · brand（2026-09-03）

## Background

为个人产品矩阵统一品牌：母品牌“归园 / guiyuan”，并落一份权威命名与内部标识对照，供跨仓协调。

## Content

- 母品牌：归园 / `guiyuan`；意象取陶渊明《归园田居》的“回到自己园子、自给自足”。
- 产品矩阵：归园大管家 `guiyuan_butler`（`private_butler`）、归园自媒体 `guiyuan_wemedia`
  （`tom_creator_os`）、归园算卦摊 `guiyuan_bazi`（`suanming_os`）、归园vibecoding
  `guiyuan_vibecoding`（`vibecoding_manager`）。
- 规则：展示名 / MCP 前缀用 `guiyuan_*`；仓库、Python 包名与 import 标识不改；内部标识改名属独立评审项。
- 沉淀：同一份 `guiyuan-brand.md` 落至 PB / VCM / Creator OS 三仓 docs。

## Verification

- VCM、PB `llms.txt` 重生成并收录该文档；VCM `check_drift` 通过（启动 1791/1800）。
- Creator OS 文档已落 `docs/03-reference/`；该仓 `llms.txt` 为其本地简式索引，不强改。
