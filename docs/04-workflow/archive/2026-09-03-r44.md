# R44 · release/internal boundaries (2026-09-03)

## Conclusion

- Defined the published payload as reusable, agent-neutral `SKILL.md` plus templates; Codex's
  `agents/openai.yaml` is an optional adapter, not the skill definition.
- Preserved host-project author ownership of `CHANGELOG.md`, `NOW.md`, and `docs/`; no blanket
  `.gitignore` rules were added for those authoritative files.
- Added required `docs/00-system` through `docs/03-reference` skeleton directories to the internal
  repository and published project template; `docs/04-workflow` remains the process layer.

## Verification

- Template and asset copies are synchronized after propagation.
- `tools/check_drift.py` and the behavior harness are the round gates.
