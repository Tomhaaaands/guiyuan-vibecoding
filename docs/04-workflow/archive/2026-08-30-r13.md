# R13 · 骨架（2026-08-30）

## 背景

用户要求脚手架骨架补依赖管理，前端全面对齐 Creator OS 的 React/Next.js 技术栈。

## 实现

1. **依赖管理**：模板根新增 `pyproject.toml`（项目元数据 + requires-python；纯 stdlib 后端，
   第三方依赖声明入口）；`_replace_placeholders` 覆盖范围从 `*.md` 扩展到 `*.md` + `*.toml`。
2. **前端骨架**：`skills/project-bootstrap/assets/frontend/{web,admin}`——Next.js 16 +
   React 19 + TS 最小工程（package.json / tsconfig / next.config.ts（rewrites /api/* →
   `API_ORIGIN` 默认 :8010）/ app/layout + page + globals.css / next-env / .gitignore）。
3. **bootstrap.py**：`_ensure_module_dirs` 对 web/admin 模块用前端骨架填充（替代空 .gitkeep）。
4. **修复 R12 bug**：`_install_precommit_gate` 插入位置破坏了 `_git_init` 的 try 块
   （bootstrap.py 语法错误，无法运行）——已还原正确结构。

## 验证

- bootstrap.py 语法检查通过；临时目录脚手架 web/admin 生成 Next.js 骨架，pyproject 占位符
  替换正确（name = 项目名）；
- suanming_os 用骨架重建前端：`npm install`（各 27 包）+ `next build` 通过；页面 200，
  代理 `/api/bazi` 返回 3 条数据；
- `check_drift` 4 对同步全绿。
