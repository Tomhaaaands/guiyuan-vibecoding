# Technical Domain Index

技术文档按责任域拆分，不与产品能力一一对应。每个文档通过 `supports` 和 `depends_on`
关联一个或多个 `docs/01-product/` 能力。

当前责任域：`analysis`、`artifacts`、`providers`、`packaging`、`frontend`、`migration`、
`orchestration`、`reflection`、`qa`、`hook`。
