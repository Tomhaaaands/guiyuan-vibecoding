# R50 · Init-time hook reminder + local Agent hook methods (2026-09-03)

## Conclusion

- Added an init-time reminder in `bootstrap.py` so the user sees that a project-scoped hook is on by
  default as a soft `SessionStart` constraint, and how to change it. Added a `--hook advisory|none`
  switch to enable or skip the hook, and a `--hook-methods` flag that prints the local Agent hook
  setup list without touching the project.
- Stored a local reference for common Agent project-scoped hook setups in
  `docs/02-technical/agent-hook-methods.md` (Codex, Claude Code, Cursor, Git, plus VCM's soft default),
  and linked it from `project-hook.md` and the `AGENTS.md` routing table so the agent reads it from
  disk instead of searching the web.
- The reminder and methods list are also available inline from the shipped skill, so the release
  does not depend on the internal repo docs. Nothing in the hook behavior changed; this only adds the
  consent/reminder surface and the local reference.

## Verification

- `python -m unittest discover -s tests -t .` -> 153 passed.
- `python tools/check_drift.py` -> passed; `tools/sync_copies.py --dry-run` -> 0 drift.
- `bootstrap.py --hook-methods` prints the local list; scaffold report shows the reminder and
  `project hook: installed`.
