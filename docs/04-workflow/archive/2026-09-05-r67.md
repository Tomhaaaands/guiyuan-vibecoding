# R67 功能模块目录与无 PB 发布验证

## 变更

- 新增 `docs/00-system/functional-module-directory.md`，用中文说明 VCM 的功能模块，并内嵌 `functionalModules` JSON 数据块。
- 模板、安装载荷与项目主页模板同步该目录；`status.html` 读取目录数据并展示功能卡片。
- 明确 PB 桥仅是 provider 适配层：VCM 不执行 embedding、不持有 embedding 模型或向量数据库；PB 关闭/不可达时使用本地关键词检索。
- 状态页渲染器对未接入目录的旧项目提供六项功能目录兜底。

## 验证

- `python tools/run_qa.py`：unittest、behavior、drift、build、architecture、package 六项全部通过。
- 完整测试：170 项通过。
- 从 `Z:\codeprojects\suanming_os` Git 状态复制临时项目，完成只改管理层的 adoption，未修改业务代码；成功生成 `status.html`，并显示六项功能目录兜底。
- `dist/guiyuan-vibecoding-0.1.0.zip` 重建成功，228 个文件，`check_package.py` 通过。

## 结论

VCM 当前可在不依赖 PB 的情况下独立运行并发布安装；PB 语义能力属于可选增强。真实 PB/NAS 联调仍需单独验证。
