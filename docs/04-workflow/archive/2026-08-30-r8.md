# R8 · release (2026-08-30)

v1.0.0 one-click install, closing the gaps between docs and template:

- Template now ships `docs/04-workflow/review-checklist.md` (per-round self-check gates) and
  `docs/04-workflow/roadmap.md` (milestones + one-line acceptance) — both previously promised
  by the methodology but missing from the scaffold; synced to `skills/project-bootstrap/assets/project`.
- `tools/check_drift.py` gains a distribution sync gate: `templates/iteration-methodology` <-> 
  `skills/project-bootstrap/assets/project` and `skills/iteration-close-loop` <-> its asset copy
  must be identical (skips gracefully in scaffolded projects). Three tool copies synced.
- New `tools/one_click_install.py` + `install.bat` / `install.sh`: Python ≥3.11 check → install
  skills → `--doctor` self-check → optional `--target <folder>` scaffold, one command.
- New `skills/luban-install/` skill (explicit-only, `$luban-install`): self-contained one-click
  installer — bundles `scripts/install.py` + copies of both skills as assets, so it installs or
  updates `iteration-close-loop` / `project-bootstrap` into `$CODEX_HOME/skills` from inside
  Codex, runs a doctor, and can scaffold a target project via the installed bootstrap script.
  `install_skills.py` and the sync gate now cover the luban asset copies.
- VERSION bumped 0.3.0 → 1.0.0; README quick start and methodology toolchain table updated
  (three copies kept identical).

Verified: `doctor` passed, `check_drift` green (sync gate included), and a temp-project scaffold
via one-click `--target` contains `review-checklist.md` + `roadmap.md`; the installed
`luban-install` skill runs standalone (doctor + scaffold) from `$CODEX_HOME`.
