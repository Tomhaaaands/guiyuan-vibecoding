interface:
  display_name: "VibeCoding Manager Install"
  short_description: "One-click install/update of VibeCoding_Manager skills + optional scaffold"
  default_prompt: "Use $vibe-coding-install to install or update the VibeCoding_Manager kit skills and optionally scaffold a project folder."
policy:
  allow_implicit_invocation: false
