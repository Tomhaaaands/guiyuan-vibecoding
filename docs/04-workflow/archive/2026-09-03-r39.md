# R39 - P4 任务图 + 就绪 + 下一任务派发

## 结论

- 新增 `tools/task_graph.py`：读取 `tasks` 权威工件（id/title/acceptance/status/priority/
  depends_on），构建依赖图，计算就绪（依赖全部 done），并选出下一个可执行任务及理由。
- 状态机：`proposed / in_progress / done / blocked`；派发按 (priority, id) 排序，跳过未就绪。
- `validate`：done 但缺 acceptance、依赖未知名任务 -> 报错。

## 验证

- `behavior_harness.py` -> 17/17 pass（新增 `task_graph` 场景：链式依赖 t1->t2->t3，初选 t1，等
  t1 done 后派发 t2，再派发 t3；就绪与缺失依赖判定正确、零误报）。
- `py_compile` ok；`check_drift.py` 绿。
- 无 Provider 依赖。
