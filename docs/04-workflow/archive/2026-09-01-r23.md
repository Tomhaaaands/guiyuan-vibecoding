# R23 · Gitee mirror publication

## Background

The Gitee domestic mirror was created under `tomhands/vibecoding-manager`, received the `main`
branch and `v0.1.0` tag, and published a `v0.1.0` Release with the same ZIP, SHA-256, and
manifest as GitHub. Documentation and install-message mirror URLs now use the actual lowercase
Gitee owner.

## Verification

- Gitee API attachment download matches the local SHA-256.
- Release assets are listed: zip, sha256, manifest.
- Public Gitee web/download URLs returned HTTP 403 from this automated environment; browser
  verification remains external/device-dependent.
