# R42 - P8 端到端 MVP

## 结论

- 新增 `tools/mvp_walkthrough.py`：一次脚本化小产品旅程串起 P2-P6——
  `分析 -> 权威工件生成 -> 一致性检查 -> 任务派发 -> 执行/验证回执 -> 经验反思`，并输出 token 预算
  门禁与整体 pass/fail。
- 默认 `local-fallback`（无密钥、可复现）；`--provider siliconflow` 可加真实模型质量。
- 这是把前面每一环串起来、用一条真实旅程给出验收证据的收官里程碑。

## 实测（local-fallback 示例）

`analysis -> product/decisions 生成（一致性 0 错） -> next_task=tasks/auth-01 -> receipt pass/done/DELIVERY
-> experience 0 -> context 132/2500 -> overall PASS`。

## 验证

- `behavior_harness.py` -> 20/20 pass（新增 `mvp_walkthrough` 场景：整条旅程通过、一致性无错、
  生成 analysis/产物、回执 pass/DELIVERY、context 不超预算）。
- `py_compile` ok；`check_drift.py` 绿。
