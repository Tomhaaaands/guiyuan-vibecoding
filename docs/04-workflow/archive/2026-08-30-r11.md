# R11 · profile (2026-08-30)

## 背景

suanming_os 端到端测试（c-end profile 脚手架）暴露 bug：profile 的 `docs_stubs` 被写到项目
根目录（`00-system/data-layer.md`、`01-product/compliance.md`）而不是 `docs/` 下。

## 实现

- `skills/project-bootstrap/scripts/bootstrap.py` `_apply_profile`：`p = target / stub` →
  `p = target / "docs" / stub`。
- 同步：skills/project-bootstrap → $CODEX_HOME 副本、vibe-coding-install 内置副本。
- suanming_os 侧：错位文件移回 `docs/00-system|01-product/`，删除根目录空壳。

## 验证

- `python tools/check_drift.py` 通过（4 对同步全绿）；
- suanming_os `check_drift` 通过。
