# R65 · composable-templates（2026-09-04）

> Full iteration record for the changelog ledger.

## Conclusion

Implemented composable project templates with a shared governance skeleton, four requested
topologies plus a composite topology, three scale profiles, and capability overlays.

## Changes

- Added machine-readable topology/scale/capability TOML profiles.
- Extended scaffold with `--template`, `--scale`, and repeatable `--capability` options.
- Generated `.guiyuan-vibecoding/project-manifest.toml` and `template.lock.toml` records.
- Added manifest-aware resolution for context budget, round rollup, llms indexing, and project home.
- Added topology/overlay tests and preserved legacy profile/default behavior.

## Verification

- `python -m tests.run_tests` — 168 tests passed.
- `python tools/check_drift.py` — passed; template and payload copies synchronized.
- Temporary `web-app` scaffold with `auth` overlay — generated successfully with manifest and
  topology directories.

