# R37 - P3 验证面：跨工件一致性检查

## 结论

- 新增 `tools/artifact_consistency.py`，落实契约 §6 的可机检规则：
  - `missing_acceptance`：accepted 的 product-spec / tasks / roadmap 缺 `## acceptance`；
  - `state_without_receipt`：project-state 处于 VERIFICATION/DELIVERY 但没有 receipts 工件；
  - `supersedes_revision_gap`：supersedes 指向目标 id 的未来版本；
  - `accepted_superseded`（warn）：被更新版本取代但仍 accepted/review。
- 引用存在性与内容哈希完整性仍由 `artifact_store.validate()` 负责；本工具补的是语义一致性。

## 验证

- `python tools/behavior_harness.py` -> 15/15 pass（新增 `artifact_consistency` 场景：干净库零错误、
  missing_acceptance / supersedes_gap / state_without_receipt 全被抓住，accepted_superseded 给 warn）。
- CLI 实测：accepted product-spec 缺验收 -> `missing_acceptance`，exit 1。
- `py_compile` ok；`check_drift.py` 绿。
