name = "vector-db"
description = "Vector-first: embeddings, collections, rebuilds"

red_lines = [
  "Vector index is a derived layer: always rebuildable from source data",
  "Collection naming follows the project convention (user-scoped when multi-tenant)",
]

constraints = [
  "Embedding model is fixed per project; index rebuilds are job-based",
]

docs_stubs = ["02-technical/vector-layer.md"]

[[modules]]
name = "kb"
keywords = "knowledge,vector,embedding,search"
code = "workers"
